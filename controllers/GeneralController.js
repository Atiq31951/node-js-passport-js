// To check the Email is unique or not
function CheckEmailUnique (req, res) {
    let email = req.body.email
}

// To check the Phone is unique or not
function CheckPhoneUnique (req, res) {
    let email = req.body.email
}

module.exports = {
    CheckEmailUnique,
    CheckPhoneUnique
}